spring.application.name=helpdesk-simulador-api
# Configura��es de conex�o com o banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/helpdesk_db?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=user
spring.datasource.password=userpass

# Configura��es de pool de conex�es
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=always

# JPA e Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Outras configura��es
# Defina o n�vel de log para depura��o (opcional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

# For�a o timezone da aplica��o para Bras�lia
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Certifique-se de [America/Sao_Paulo] est� configurado no seu sistema operacional
server.timezone=America/Sao_Paulo
